// @flow
/**
 * Flowtype definitions for node-forge
 * Generated by Flowgen from a Typescript Definition
 * Flowgen v1.0.0
 * Author: [Joar Wilk](http://twitter.com/joarwilk)
 * Repo: http://github.com/joarwilk/flowgen
 */

declare module 'node-forge' {

    declare     var npm$namespace$pki: {
        privateKeyToPem: typeof pki$privateKeyToPem,
        publicKeyToPem: typeof pki$publicKeyToPem,
    }
    declare     export type pki$PEM = string;

    declare     export type pki$Key = any;

    declare     export interface pki$KeyPair {
        publicKey: pki$Key,
            privateKey: pki$Key
    }

    declare     export function pki$privateKeyToPem(key: pki$Key, maxline?: number): pki$PEM

    declare     export function pki$publicKeyToPem(key: pki$Key, maxline?: number): pki$PEM


    declare     var npm$namespace$rsa: {
        generateKeyPair: typeof rsa$generateKeyPair,
    }
    declare     export interface rsa$GenerateKeyPairOptions {
        bits?: number,
            e?: number,
            workerScript?: string,
            workers?: number,
            workLoad?: number,
            prng?: any,
            algorithm?: string
    }

    declare     export function rsa$generateKeyPair(
        bits?: number,
        e?: number,
        callback?: (err: Error, keypair: pki$KeyPair) => void): pki$KeyPair
}