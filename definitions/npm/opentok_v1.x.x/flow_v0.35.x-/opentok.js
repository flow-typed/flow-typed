// @flow
/**
 * Flowtype definitions for opentok
 * Generated by Flowgen from a Typescript Definition
 * Flowgen v1.0.0
 * Author: [Joar Wilk](http://twitter.com/joarwilk)
 * Repo: http://github.com/joarwilk/flowgen
 */

declare module 'opentok' {
    declare     export type OpenTok$OutputMode = "composed" | "individual";

    declare     export type OpenTok$ArchiveStatus = "available" |
        "expired" |
        "failed" |
        "paused" |
        "started" |
        "stopped" |
        "uploaded";

    declare     export interface OpenTok$Archive {
        createdAt: number,
            duration: string,
            id: string,
            name: string,
            partnerId: string,
            reason: string,
            sessionId: string,
            size: number,
            status: OpenTok$ArchiveStatus,
            hasAudio: boolean,
            hasVideo: boolean,
            outputMode: OpenTok$OutputMode,
            url: string
    }

    declare     export interface OpenTok$ArchiveOptions {
        name?: string,
            hasAudio?: boolean,
            hasVideo?: boolean,
            outputMode?: OpenTok$OutputMode
    }

    declare     export type OpenTok$MediaMode = "relayed" | "routed";

    declare     export type OpenTok$ArchiveMode = "manual" | "always";

    declare     export interface OpenTok$SessionOptions {
        mediaMode?: OpenTok$MediaMode,
            archiveMode?: OpenTok$ArchiveMode,
            location?: string
    }

    declare     export interface OpenTok$Session {
        sessionId: string
    }

    declare     export type OpenTok$Token = string;

    declare     export type OpenTok$Role = "subscriber" | "publisher" | "moderator";

    declare     export interface OpenTok$TokenOptions {
        role?: OpenTok$Role,
            data?: string,
            expireTime?: number
    }

    declare     export interface OpenTok$ListArchivesOptions {
        count?: number,
            offset?: number
    }
    declare module.exports: typeof OpenTok
}